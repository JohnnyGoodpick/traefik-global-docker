# Let's Encrypt email (required for certificate expiry warnings)
ACME_EMAIL=admin@yourdomain.com

# Dashboard credentials (localhost:9090, access via Tailscale)
# Generate hash: docker run --rm httpd:2-alpine htpasswd -nB admin
# IMPORTANT: Double all $ signs (e.g., $2y$ becomes $$2y$$)
DASHBOARD_USER=admin
DASHBOARD_PASSWORD_HASH=$$2y$$05$$your_hash_here
